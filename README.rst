Dockerfile used to build `nunogrl/node-ci <https://hub.docker.com/r/nunogrl/node-ci>`_

v3
==

**Add docker and docker-compose**

reasoning: allows to build projects by simply calling docker-compose.

Ensure to invoke docker-compose as

.. code-block:: CONSOLE

    $ docker compose logs -f --no-color


v2
==

**The smallest image to start with**
